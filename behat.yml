default:
  extensions:
    Behat\MinkExtension:
      default_session: goutte
      goutte: ~
  suites:
    defaults:
      contexts:
        - SecurityContext
        - AuthenticationContext
        - ModuleContext
        - PagesContext